<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Date_Time__c</fullName>
    <description>Legacy field for deletion whenever the legacy components that use this are removed. We do not track due time, just due date, so this is redundant. 

Custom field for Date / Time of a task. The default SF field is Due Date only.</description>
    <label>zDel - Due Date / Time</label>
    <required>false</required>
    <type>DateTime</type>
</CustomField>
