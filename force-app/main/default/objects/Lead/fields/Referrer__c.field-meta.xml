<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Referrer__c</fullName>
    <deleteConstraint>Restrict</deleteConstraint>
    <description>This field captures the referrer. It is a lookup to the account where we store various types of referrer like people (clients, colleagues, or introducers) and businesses. Maps to an equivalent field on the account.</description>
    <inlineHelpText>The client, colleague, introducer, or business that made the referral.</inlineHelpText>
    <label>Referrer</label>
    <referenceTo>Account</referenceTo>
    <relationshipLabel>Referred leads</relationshipLabel>
    <relationshipName>ReferredLeads</relationshipName>
    <required>false</required>
    <trackFeedHistory>false</trackFeedHistory>
    <trackHistory>false</trackHistory>
    <type>Lookup</type>
</CustomField>
