<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>DocumentProofStatusAndUrl</fullName>
    <active>true</active>
    <description>Ensures the document proof URL is only specified if we also have the correct status indicated.</description>
    <errorConditionFormula>!$Setup.SystemConfiguration__c.ValidationBypass__c
&amp;&amp;
(NOT(ISBLANK(DocumentUrl__c))
&amp;&amp;
NOT(ISPICKVAL(DocumentProof__c, &quot;RECEIVED_SAVED&quot;)))
||
(ISBLANK(DocumentUrl__c)
&amp;&amp;
ISPICKVAL(DocumentProof__c, &quot;RECEIVED_SAVED&quot;))</errorConditionFormula>
    <errorMessage>Document proof must be marked as received and saved to Drive at the same time the URL is specified.</errorMessage>
</ValidationRule>
