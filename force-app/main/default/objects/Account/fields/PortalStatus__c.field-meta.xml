<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>PortalStatus__c</fullName>
    <description>Business use: used to accurately show the clients portal status through 1 field. If client has not migrated to cognito it shows their old portal status, but if they have it shows their new status 
Tech use: n/a
Data inputters: system/formula
Data users: All SF users</description>
    <externalId>false</externalId>
    <formula>IF(ISPICKVAL(PortalAccountStatus__c, &quot;&quot;), TEXT( OW_Portal_Activated__c ), TEXT(PortalAccountStatus__c))</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>Portal status</label>
    <required>false</required>
    <trackHistory>false</trackHistory>
    <type>Text</type>
    <unique>false</unique>
</CustomField>
