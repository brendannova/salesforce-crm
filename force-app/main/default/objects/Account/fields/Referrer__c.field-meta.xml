<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Referrer__c</fullName>
    <deleteConstraint>SetNull</deleteConstraint>
    <description>To store the person, colleague or introducer who referred this client. Could also store a business if client came through a company rollout. Populated on lead and pull through on conversion</description>
    <label>Referrer</label>
    <referenceTo>Account</referenceTo>
    <relationshipLabel>Referrer</relationshipLabel>
    <relationshipName>Referrer</relationshipName>
    <required>false</required>
    <trackFeedHistory>false</trackFeedHistory>
    <trackHistory>false</trackHistory>
    <type>Lookup</type>
</CustomField>
