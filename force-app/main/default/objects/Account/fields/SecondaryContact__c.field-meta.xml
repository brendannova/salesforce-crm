<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>SecondaryContact__c</fullName>
    <deleteConstraint>SetNull</deleteConstraint>
    <description>Business use: Stores the second client in a household to use for easy lookup when needed.
Tech use: Stores the person account of a household  where role on the related account contact relationship is client and the primary contact is unchecked. Updated via account contact relationship after save flow.</description>
    <label>Secondary contact</label>
    <referenceTo>Account</referenceTo>
    <relationshipLabel>Secondary contact</relationshipLabel>
    <relationshipName>SecondaryContact</relationshipName>
    <required>false</required>
    <trackFeedHistory>false</trackFeedHistory>
    <trackHistory>false</trackHistory>
    <type>Lookup</type>
</CustomField>
