<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>StatusDisplay__c</fullName>
    <description>This formula field combines the status and sub-status, 2 separate fields, into a single value for display purposes. Often the sub-status will be blank so we don&apos;t want to show that field on pages or highlights panels where it&apos;s not necessary. As these fields should only be controlled by automation too, it allows us to remove the potentially editable fields from page views.</description>
    <externalId>false</externalId>
    <formula>TEXT(FinServ__Status__c) + IF(ISPICKVAL(OW_sub_status__c, &quot;&quot;), &quot;&quot;, &quot; | &quot; + TEXT(OW_sub_status__c))</formula>
    <label>Status</label>
    <required>false</required>
    <trackHistory>false</trackHistory>
    <type>Text</type>
    <unique>false</unique>
</CustomField>
