<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>StatusSendLoaToProvider</fullName>
    <active>true</active>
    <description>Shows an error if a user attempts to mark this task as completed but the relevant fields on the LOA have not been completed</description>
    <errorConditionFormula>!$Setup.SystemConfiguration__c.ValidationBypass__c &amp;&amp;
AND(
 OW_task_information_identifier__c = &quot;send_loa_to_provider&quot;,
ISPICKVAL(Status,&quot;Completed&quot;), 
ISNULL(Related_LOA__r.Date_LOA_sent_to_provider__c),
ISPICKVAL(Related_LOA__r.Format_Sent_to_Provider__c, &quot;&quot;)
)</errorConditionFormula>
    <errorMessage>You need to add the date and format in which the LOA was sent to the provider</errorMessage>
</ValidationRule>
